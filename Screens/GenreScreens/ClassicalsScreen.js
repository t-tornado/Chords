import React, { useEffect } from "react";
import { StyleSheet, View, FlatList, Text } from "react-native";

export function DumyClassicalsScreen(){
  return(
    <View style={{
      flex:1,
      justifyContent: "center"
    }} >
      <Text style={{
        fontSize: 15,
        color: '#ffffff80'
      }} >Classicals Screen</Text>
    </View>
  )
}